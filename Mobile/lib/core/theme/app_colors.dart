import 'dart:ui';

class AppColors {
  AppColors._();

  // ─── Primary ───
  static const primary = Color(0xFF1A6B52);
  static const primaryLight = Color(0xFF2D8E6E);
  static const primaryDark = Color(0xFF145542);

  // ─── Secondary ───
  static const secondary = Color(0xFF2563EB);
  static const secondaryLight = Color(0xFF3B82F6);

  // ─── Gradient ───
  static const gradientStart = Color(0xFF1B3A5C);
  static const gradientMiddle = Color(0xFF1A6B52);
  static const gradientEnd = Color(0xFF2D8E6E);

  // ─── Surface ───
  static const surface = Color(0xFFFFFFFF);
  static const surfaceAlt = Color(0xFFF5F7FA);
  static const scaffoldLight = Color(0xFFF8F9FB);

  // ─── Text ───
  static const textPrimary = Color(0xFF1A1A2E);
  static const textSecondary = Color(0xFF6B7280);
  static const textHint = Color(0xFF9CA3AF);

  // ─── Status ───
  static const success = Color(0xFF10B981);
  static const warning = Color(0xFFF59E0B);
  static const error = Color(0xFFEF4444);
  static const info = Color(0xFF3B82F6);

  // ─── Dark Mode ───
  static const darkBackground = Color(0xFF1A1A2E);
  static const darkSurface = Color(0xFF252540);
  static const darkSurfaceAlt = Color(0xFF2E2E4A);
  static const darkTextPrimary = Color(0xFFFFFFFF);
  static const darkTextSecondary = Color(0xFFA0A3BD);

  // ─── Misc ───
  static const divider = Color(0xFFE5E7EB);
  static const shadow = Color(0x1A000000);
  static const starRating = Color(0xFFF59E0B);
}
